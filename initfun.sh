#!/bin/bash
echo "<----------installing in background, Have some popcorns till then---------->"
run="sudo apt install nyancat && sudo apt install sl && sudo apt install cmatrix && sudo apt isntall figlet & 2>/dev/null" 